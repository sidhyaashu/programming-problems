#include<stdio.h>
#include<stdlib.h>

//define a structure for a node 

struct Node{
    int data;
    struct Node* next;
};

//function to create a new node 
struct Node* createNode(int value){
    
}